New features:
) move all photos (buttons to select "all", "current page", "visible on screen")
) sorting (by order, by date, by likes, shuffle)
) by default "inverse order" enabled for "Saved" album only
) more tooltips (duplicates album)
) progress bar label?

Etc:
) update help wiki page

) test on all modern browsers: chrome, firefox, edge, ie
) style tuning: better loadding gif, fonts, etc...
) video "how to use"


No go:
) arrow bg image for album select form
) donate button (disable ads)